"use client";
/* eslint-disable @next/next/no-img-element */
import { getArtworkDisplays } from "@/lib/image";
export default function ArtworkReviewCard({item}:{item:Record<string,unknown>}){const images=getArtworkDisplays({imageUrl:String(item.imageUrl||""),thumbnailUrl:String(item.thumbnailUrl||""),displayUrl:String(item.displayUrl||""),images:String(item.images||"[]"),imageVariants:String(item.imageVariants||"[]")});return <><div className={`grid gap-2 ${images.length>1?"grid-cols-2":"grid-cols-1"}`}>{images.map((src,index)=><div key={src} className="relative"><img src={src} className="aspect-square w-full rounded-2xl object-cover" alt={`${String(item.title||"作品")} 第${index+1}张`}/>{images.length>1&&<span className="absolute bottom-2 left-2 rounded-full bg-black/70 px-2 py-1 text-xs text-white">{index+1}/{images.length}</span>}</div>)}</div><b className="mt-3 block">{String(item.title)}</b><p className="text-xs text-stone-500">共 {images.length} 张图片</p>{item.copyrightRisk&&<p className="font-bold text-red-600">⚠ 侵权风险</p>}</>}

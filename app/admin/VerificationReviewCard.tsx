"use client";
/* eslint-disable @next/next/no-img-element */
function images(value:unknown){try{const parsed=JSON.parse(String(value||"[]"));return Array.isArray(parsed)?parsed.map(String).filter(Boolean):[]}catch{return[]}}
export default function VerificationReviewCard({item}:{item:Record<string,unknown>}){const list=images(item.representativeImages);return <><b className="block text-lg">{String(item.displayName||"画师")}</b>{item.realName&&<p className="mt-1 text-sm text-stone-500">实名：{String(item.realName)}</p>}<div className="mt-4 grid grid-cols-2 gap-2 sm:grid-cols-3">{list.map((src,index)=><a key={`${src}-${index}`} href={src} target="_blank" rel="noreferrer" className="relative block overflow-hidden rounded-xl"><img src={src} alt={`认证代表图 ${index+1}`} className="aspect-square w-full object-cover"/><span className="absolute bottom-2 left-2 rounded-full bg-black/70 px-2 py-1 text-xs text-white">{index+1}/{list.length}</span></a>)}</div><p className="mt-2 text-xs text-stone-500">共提交 {list.length} 张代表图，点击可查看原图</p>{!list.length&&<p className="mt-3 rounded-xl bg-red-50 p-3 text-sm text-red-600">这条申请没有可用代表图，请驳回后让画师重新提交。</p>}</>}

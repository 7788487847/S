"use client";import{Suspense,useState}from"react";import{useSearchParams}from"next/navigation";function Form(){const t=useSearchParams().get("token"),[password,setPassword]=useState(""),[msg,setMsg]=useState("");async function go(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/auth/reset-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token:t,password})}),d=await r.json();setMsg(d.message||d.error)}return <form onSubmit={go} className="w-full max-w-md space-y-4 rounded-3xl bg-white p-8 shadow"><h1 className="text-2xl font-bold">设置新密码</h1><input className="input" type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="至少8位"/><button className="btn w-full">确认重置</button>{msg&&<p>{msg}</p>}</form>}export default function Page(){return <main className="grid min-h-screen place-items-center p-5"><Suspense><Form/></Suspense></main>}

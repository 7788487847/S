"use client";import Link from"next/link";import{useRouter}from"next/navigation";import{useState}from"react";import Header from"@/components/Header";export default function Page(){const router=useRouter(),[f,setF]=useState({email:"",password:""}),[err,setErr]=useState("");function clear(){localStorage.removeItem("palette_token");localStorage.removeItem("palette_user");setF({email:"",password:""});router.replace("/login")}async function go(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(f)}),d=await r.json();if(!r.ok)return setErr(d.error);localStorage.setItem("palette_token",d.token);localStorage.setItem("palette_user",JSON.stringify(d.user));dispatchEvent(new Event("palette-auth"));router.push("/dashboard")}return <><Header/><main className="grid min-h-[75vh] place-items-center p-5"><form onSubmit={go} className="w-full max-w-md space-y-4 rounded-3xl bg-white p-8 shadow"><h1 className="text-3xl font-bold">登录灵犀</h1><input className="input" type="email" placeholder="邮箱" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><input className="input" type="password" placeholder="密码" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/><button className="btn w-full">登录</button><div className="flex justify-between text-sm"><button type="button" onClick={clear}>切换账号</button><Link href="/forgot-password">忘记密码？</Link></div>{err&&<p className="text-red-600">{err}</p>}</form></main></>}

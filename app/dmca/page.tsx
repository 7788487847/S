"use client";import{useState}from"react";import Header from"@/components/Header";import Footer from"@/components/Footer";export default function Page(){const[msg,setMsg]=useState("");async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),files=Array.from(f.getAll("proofFiles")).filter(x=>x instanceof File)as File[],proofImages:string[]=[];if(files.length){const fd=new FormData();files.forEach(x=>fd.append("images",x));const u=await fetch("/api/upload",{method:"POST",headers:{authorization:`Bearer ${localStorage.getItem("palette_token")||""}`},body:fd});if(u.ok)proofImages.push(...(await u.json()).urls)}const b=Object.fromEntries(f);const r=await fetch("/api/complaints",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...b,proofImages})}),d=await r.json();setMsg(d.message||d.error)}return <><Header/><main className="mx-auto max-w-2xl px-5 py-14"><form onSubmit={go} className="space-y-4 rounded-3xl bg-white p-8 shadow"><h1 className="text-3xl font-bold">侵权投诉</h1><p className="text-sm text-orange-700">平台在收到侵权通知后24小时内处理，对投诉成立的作品立即下线。</p><input className="input" name="name" placeholder="投诉人姓名" required/><input className="input" name="email" type="email" placeholder="联系邮箱" required/><textarea className="input" name="targetUrls" placeholder="被投诉作品URL，可填写多个，每行一个" required/><textarea className="input" name="ownershipProof" placeholder="权属证明文字描述" required/><input className="input" name="proofFiles" type="file" multiple accept="image/jpeg,image/png,image/webp"/><textarea className="input min-h-32" name="description" placeholder="侵权说明" required/><button className="btn">提交投诉</button>{msg&&<p>{msg}</p>}</form></main><Footer/></>}

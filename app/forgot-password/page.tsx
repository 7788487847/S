"use client";import{useState}from"react";import Header from"@/components/Header";export default function Page(){const[email,setEmail]=useState(""),[msg,setMsg]=useState("");async function go(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})}),d=await r.json();setMsg(`${d.message}${d.resetUrl?`：${d.resetUrl}`:""}`)}return <><Header/><main className="grid min-h-[70vh] place-items-center p-5"><form onSubmit={go} className="w-full max-w-md space-y-4 rounded-3xl bg-white p-8 shadow"><h1 className="text-2xl font-bold">找回密码</h1><input className="input" type="email" placeholder="注册邮箱" value={email} onChange={e=>setEmail(e.target.value)} required/><button className="btn w-full">发送重置链接</button>{msg&&<p className="break-all text-sm text-orange-700">{msg}</p>}</form></main></>}

"use client";import Image,{ImageProps}from"next/image";import{useState}from"react";export default function FadeImage({className="",alt,sizes="(max-width: 768px) 100vw, 50vw",...props}:ImageProps){const[loaded,setLoaded]=useState(false);return <Image {...props} alt={alt} sizes={sizes} unoptimized={typeof props.src==="string"&&props.src.endsWith(".gif")} onLoad={()=>setLoaded(true)} className={`fade-image ${loaded?"loaded":""} ${className}`}/>} 

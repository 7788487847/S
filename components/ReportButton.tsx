"use client";import{useState}from"react";export default function ReportButton({type,id}:{type:"artwork"|"artist";id:number}){const[open,setOpen]=useState(false),[msg,setMsg]=useState("");async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch("/api/reports",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({targetType:type,targetId:id,reason:f.get("reason"),description:f.get("description"),reporterEmail:f.get("email")})}),d=await r.json();setMsg(d.message||d.error);if(r.ok)setTimeout(()=>setOpen(false),1000)}return <><button onClick={()=>setOpen(true)} className="text-sm text-stone-500 underline">举报</button>{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/40 p-5"><form onSubmit={go} className="w-full max-w-md space-y-4 rounded-3xl bg-white p-7"><h2 className="text-xl font-bold">提交举报</h2><select name="reason" className="input">{["侵权","色情","仿冒","引流","其他"].map(x=><option key={x}>{x}</option>)}</select><textarea name="description" className="input" placeholder="说明"/><input name="email" type="email" className="input" placeholder="联系邮箱" required/><button className="btn w-full">提交</button><button type="button" onClick={()=>setOpen(false)} className="w-full">取消</button>{msg&&<p>{msg}</p>}</form></div>}</>}

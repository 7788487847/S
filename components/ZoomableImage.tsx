"use client";
/* eslint-disable @next/next/no-img-element */
import { useEffect,useState } from "react";
type Props={src:string;alt:string;className?:string;badge?:string};export default function ZoomableImage({src,alt,className="",badge}:Props){const[open,setOpen]=useState(false);useEffect(()=>{if(!open)return;const close=(event:KeyboardEvent)=>{if(event.key==="Escape")setOpen(false)};document.body.style.overflow="hidden";addEventListener("keydown",close);return()=>{document.body.style.overflow="";removeEventListener("keydown",close)}},[open]);return <><button type="button" onClick={()=>setOpen(true)} aria-label={`查看大图：${alt}`} className="relative block w-full cursor-zoom-in overflow-hidden bg-transparent p-0 text-left"><img src={src} alt={alt} draggable="true" className={className}/>{badge&&<span className="absolute right-3 top-3 rounded-full bg-black/70 px-2 py-1 text-xs text-white">{badge}</span>}</button>{open&&<div role="dialog" aria-modal="true" aria-label="图片查看器" onClick={()=>setOpen(false)} className="fixed inset-0 z-[100] grid place-items-center bg-black/90 p-3 sm:p-8"><button type="button" onClick={()=>setOpen(false)} aria-label="关闭大图" className="absolute right-4 top-4 z-10 grid size-11 place-items-center rounded-full bg-white/15 text-3xl text-white">×</button><div className="absolute left-1/2 top-5 -translate-x-1/2 rounded-full bg-black/50 px-4 py-2 text-sm text-white">大图查看</div><img onClick={event=>event.stopPropagation()} src={src} alt={alt} draggable="true" className="max-h-[90vh] max-w-[94vw] object-contain"/></div>}</>}

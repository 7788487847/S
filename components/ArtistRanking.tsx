import Link from"next/link";type Artist={id:number;displayName:string;avatarUrl:string|null;artistNumber:number|null;likes:number;favorites:number;inquiries:number;score:number};export default function ArtistRanking({artists}:{artists:Artist[]}){if(!artists.length)return <div className="rounded-3xl border border-dashed p-16 text-center text-stone-500">认证画师榜正在积累中 ✦</div>;return <div className="grid gap-4 md:grid-cols-2">{artists.map((a,i)=><Link key={a.id} href={`/u/${a.id}`} className="flex items-center gap-4 rounded-2xl bg-white p-5 shadow-sm transition hover:-translate-y-1 dark:bg-[#1a1a1a]"><span className={`grid size-10 place-items-center rounded-full font-bold ${i<3?"brand-bg text-white":"bg-stone-100 text-stone-500"}`}>{i+1}</span><div className="min-w-0 flex-1"><b>{a.displayName}</b>{a.artistNumber&&<small className="ml-2 brand-text">No.{String(a.artistNumber).padStart(2,"0")}</small>}<p className="mt-1 text-xs text-stone-500">♥ {a.likes} 喜欢 · ★ {a.favorites} 收藏 · ✉ {a.inquiries} 咨询</p></div><strong className="brand-text">{a.score}</strong></Link>)}</div>}
